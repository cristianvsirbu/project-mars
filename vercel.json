{
	"version": 2,
	"routes": [
		{ "handle": "filesystem" },
		{ "src": "/.*", "dest": "/index.html" }
	],
	"build": {
		"env": {
			"NODE_ENV": "production"
		},
		"command": "npm install --legacy-peer-deps && npm run build"
	}
}
