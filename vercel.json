{
	"version": 2,
	"routes": [
		{ "handle": "filesystem" },
		{ "src": "/.*", "dest": "/index.html" }
	],
	"build": {
		"env": {
			"NODE_ENV": "production",
			"NODE_OPTIONS": "--legacy-peer-deps"
		}
	}
}
